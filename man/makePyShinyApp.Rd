% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makePyShinyApp.R
\name{makePyShinyApp}
\alias{makePyShinyApp}
\title{Create a Complete pyShinyCell Shiny Application}
\usage{
makePyShinyApp(
  obj,
  scConf = NULL,
  shiny.dir = "shinyApp/",
  shiny.title = "pyShinyCell App",
  shiny.prefix = "sc1",
  enable_tabs = c("de1", "de2", "gsea", "correlation", "tcr"),
  gex.assay = NA,
  gex.slot = c("data", "scale.data", "counts"),
  gene.mapping = FALSE,
  default.gene1 = NA,
  default.gene2 = NA,
  default.multigene = NA,
  default.dimred = NA,
  python_env = "pyShinyCell",
  setup_python = TRUE,
  generate_h5ad = TRUE,
  compress_h5ad = TRUE,
  remove_assays = NULL,
  verbose = TRUE
)
}
\arguments{
\item{obj}{input single-cell object for Seurat (v3+) / SingleCellExperiment
data or input file path for h5ad / loom files}

\item{scConf}{shinycell config data.table. If NULL (default), automatically
created from obj using createPyConfig()}

\item{shiny.dir}{directory where the Shiny app will be created.
Default is "shinyApp/"}

\item{shiny.title}{title for the Shiny application.
Default is "pyShinyCell App"}

\item{shiny.prefix}{file prefix for generated data files.
Default is "sc1"}

\item{enable_tabs}{character vector of analysis tabs to include.
Options: c("de1", "de2", "gsea", "correlation", "tcr").
Default is all tabs.}

\item{gex.assay}{assay to use for gene expression. Defaults to "RNA" for
Seurat, "logcounts" for SCE.}

\item{gex.slot}{slot to use (Seurat only). Default is "data".}

\item{gene.mapping}{if TRUE, converts Ensembl IDs to gene symbols.
Can also be a named vector for custom mapping.}

\item{default.gene1}{primary default gene to display}

\item{default.gene2}{secondary default gene to display}

\item{default.multigene}{character vector of default genes for multi-gene plots}

\item{default.dimred}{character vector of two default dimension reductions}

\item{python_env}{name of Python environment to setup/use.
Default is "pyShinyCell"}

\item{setup_python}{if TRUE (default), runs setupPythonEnv() to ensure
Python is configured. Set to FALSE if already configured.}

\item{generate_h5ad}{if TRUE (default), generates H5AD file for Python access}

\item{compress_h5ad}{if TRUE (default), applies gzip compression to H5AD}

\item{remove_assays}{character vector of Seurat assays to remove before
H5AD conversion (e.g., c("integrated", "SCT"))}

\item{verbose}{if TRUE (default), prints progress messages}
}
\value{
Invisibly returns a list with paths to all created files and
  directories.
}
\description{
Complete end-to-end function that generates a fully-functional pyShinyCell
Shiny application. This is the main API function users will call.
}
\details{
This function performs a complete 4-step workflow:
1. **Config Creation**: Create or validate scConf
2. **Data Generation**: Call makePyShinyFiles() to generate data files
3. **Code Generation**: Call makePyShinyCode() to generate Shiny app code
4. **Python Setup**: Optional Python environment configuration

The resulting Shiny application in `shiny.dir` is ready to run with:
```r
shiny::runApp("path/to/shinyApp")
```
}
\section{Requirements}{

- Seurat (v3+) or SingleCellExperiment object with metadata and embeddings
- OR H5AD file path
- Sufficient RAM to load the full object (8-16GB for typical datasets)
}

\section{Generated Files}{

Data files (from makePyShinyFiles):
- `<prefix>conf.rds`, `<prefix>meta.rds`, `<prefix>gene.rds`
- `<prefix>gexpr.h5` (gene expression matrix)
- `<prefix>def.rds` (default settings)
- `<prefix>gexpr.h5ad` (H5AD format for Python)
- `<prefix>maxlvl.rds` (max expression per gene)

Code files (from makePyShinyCode):
- `server.R` (Shiny server logic)
- `ui.R` (Shiny UI definitions)
- `global.R` (initialization and data loading)
- `util.R` (plotting and analysis functions)
- `util_palette.R` (color palettes)
}

\examples{
# This is typically called internally or requires setup.

}
